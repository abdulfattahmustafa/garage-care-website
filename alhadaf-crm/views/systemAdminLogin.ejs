<!doctype html>
<html lang="ar" dir="rtl">
<head>
  <%- include('partials/head', { title: 'دخول مدير النظام' }) %>
</head>
<body>
  <div class="login-wrap">
    <div class="login-card">
      <div class="logo-mark"></div>
      <h1>مدير النظام</h1>
      <p class="sub">لوحة إدارة المعارض المشتركة — ليست جزءًا من أي معرض</p>
      <% if (error) { %>
        <div class="flash flash-error"><%= error %></div>
      <% } %>
      <form method="POST" action="/system-admin/login">
        <div class="field" style="text-align:right">
          <label>اسم المستخدم</label>
          <input type="text" name="username" required autofocus autocapitalize="off" autocorrect="off">
        </div>
        <div class="field" style="text-align:right">
          <label>كلمة المرور</label>
          <input type="password" name="password" required>
        </div>
        <button type="submit" class="btn btn-primary">دخول</button>
      </form>
      <p class="sub" style="margin-top:16px"><a href="/login">تسجيل دخول معرض عادي</a></p>
    </div>
  </div>
</body>
</html>
